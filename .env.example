# CountryLab requires no environment variables for local development or production.
# Do not add API keys to the client bundle. If a future feature needs credentials,
# keep them server-side and document only variable names here.
